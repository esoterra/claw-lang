export func le(left: u32, right: u32) -> u32 {
    if left <= right {
        return left;
    }
    return right + 2;
}

export func ge(left: u32, right: u32) -> u32 {
    if left >= right {
        return left;
    }
    return right + 2;
}